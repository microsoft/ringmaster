#ifndef SPLIT_HH
#define SPLIT_HH

#include <string>
#include <vector>

std::vector<std::string> split(const std::string & str,
                               const std::string & separator);

#endif /* SPLIT_HH */
